/** @format */

// player.js

// Function to initialize the player
function initializePlayer(canvas) {
  const player = {
    x: 50,
    y: canvas.height - 50,
    width: 30,
    height: 30,
    speed: 5,
    jumping: false,
    jumpHeight: 150,
    jumpVelocity: 10,
    maxJumps: 2, // Maximum consecutive jumps
    jumps: 0, // Current consecutive jumps
  };

  return player;
}

// Function to update the player's position
function updatePlayer(player) {
  // Your player update logic will go here
}

// Function to render the player
function renderPlayer(ctx, player) {
  ctx.fillStyle = "blue";
  ctx.fillRect(player.x, player.y, player.width, player.height);
}

// Export the player module functions
export { initializePlayer, updatePlayer, renderPlayer };
